# Copy to `.env` for local development (`npx expo start`).
# The URL is FieldSynk's Supabase project. The anon/publishable key is a PUBLIC
# client key (RLS protects the data) — copy it from the Supabase dashboard:
# Project Settings -> API -> Project API keys -> anon / publishable.
EXPO_PUBLIC_SUPABASE_URL=https://vzuxbgxtqnzpyjgpiswg.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=REPLACE_WITH_FIELDSYNK_PUBLISHABLE_KEY
